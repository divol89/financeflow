import type { ReactNode } from 'react';

export function TokenContextProvider({ children }: { children: ReactNode }): JSX.Element;
export function useTokenContext(): unknown;
declare const TokenContext: unknown;
export default TokenContext;
