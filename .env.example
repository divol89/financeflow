# Optional: public Matrix dashboard proxy to the IO bot backend.
# Use a reachable HTTPS URL in production (Vercel cannot reach your Mac's 127.0.0.1).
# Do not put wallet/private keys here.
IO_BOT_API_BASE_URL=http://127.0.0.1:8787
